<UserControl x:Class="DevUtil.Plugins.ProjectManager.ProjectManagerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1E1E1E">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 상단 툴바 -->
        <Border Grid.Row="0" Background="#2D2D30" Padding="10" BorderBrush="#3F3F46" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <Button Content="새 프로젝트" Padding="10,5" Margin="0,0,10,0" 
                        Background="#007ACC" Foreground="White" BorderThickness="0"/>
                <Button Content="편집" Padding="10,5" Margin="0,0,10,0"
                        Background="#0E639C" Foreground="White" BorderThickness="0"/>
                <Button Content="삭제" Padding="10,5"
                        Background="#C50500" Foreground="White" BorderThickness="0"/>
            </StackPanel>
        </Border>
        
        <!-- 프로젝트 그리드 -->
        <DataGrid Grid.Row="1" 
                  AutoGenerateColumns="False"
                  Background="#252526"
                  RowBackground="#1E1E1E"
                  AlternatingRowBackground="#252526"
                  BorderBrush="#3F3F46"
                  BorderThickness="1"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  Margin="10"
                  CanUserAddRows="False">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#2D2D30"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Padding" Value="10,5"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="프로젝트명" Width="200" Binding="{Binding ProjectName}"/>
                <DataGridTextColumn Header="설명" Width="300" Binding="{Binding Description}"/>
                <DataGridTextColumn Header="상태" Width="100" Binding="{Binding Status}"/>
                <DataGridTextColumn Header="시작일" Width="120" Binding="{Binding StartDate}"/>
                <DataGridTextColumn Header="종료일" Width="120" Binding="{Binding EndDate}"/>
                <DataGridTextColumn Header="진행률" Width="80" Binding="{Binding Progress}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>